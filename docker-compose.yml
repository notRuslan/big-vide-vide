services:
  web:
    build: .
    ports:
      - "8080:3000"
    restart: unless-stopped
    environment:
      - PORT=3000
      - DB_PATH=/app/data/todos.db
    volumes:
      - sqlite_data:/app/data

volumes:
  sqlite_data:
